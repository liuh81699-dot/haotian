# Workspace Projects

This repository now keeps the two projects in separate top-level directories:

- `bi-page-compare/`: BI dual-environment page/card comparison tool
- `guandata-custom-chart/`: GuanData custom chart skill assets

## Usage

For the BI page compare project:

```bash
cd bi-page-compare
```

For the GuanData custom chart project:

```bash
cd guandata-custom-chart
```
